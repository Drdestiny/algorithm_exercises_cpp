#include <iostream>
#include <vector>

using namespace std;

// Leetcode 85: 最大矩形

class Solution {
public:
    int maximalRectangle(vector<vector<char>> &matrix) {
        int rows = matrix.size();
        if (rows == 0)
            return 0;
        int cols = matrix[0].size();
    }
};